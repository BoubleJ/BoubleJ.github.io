import * as styles from "./Footer.css";

export default function Footer() {
  return (
    // 내용은 추후 채운다. margin-top: auto로 본문을 밀어 올리는 역할이 있어 요소 자체는 유지한다.
    <footer className={styles.footerWrapper} />
  );
}
