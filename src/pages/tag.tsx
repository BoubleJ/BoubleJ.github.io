import React from "react";

export default function Tag() {
  return (
    <div>
      아니 뭐는 되고 뭐는 안되는 이유가 뭐냐,,?? 여긴 태그 목록 이 존재하는 곳
      지금 home 페이지에서 tag 클릭하면 필터링되는 것처럼 그대로 구현하면 된다.
    </div>
  );
}
