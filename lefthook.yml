pre-commit:
  commands:
    check:
      run: npx biome check --write {staged_files}
      glob: "**/*.{ts,tsx,js,jsx,css,astro}"
      stage_fixed: true

pre-push:
  commands:
    typecheck:
      run: npm run typecheck
