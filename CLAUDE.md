# BoubleJ.github.io

Astro 기반 개인 기술 블로그입니다.

## 구조

- 포스트는 `src/content/<제목>/index.mdx` 에 둡니다.
- 이미지는 포스트 디렉터리 안에 두고 `![alt](<./파일명.png>)` 로 참조합니다.
- 썸네일도 포스트 디렉터리 안에 `thumbnail.png` 로 두고 `./thumbnail.png` 로 참조합니다. `src/content.config.ts` 의 `image()` 스키마가 webp 변환과 해시를 붙입니다.
- GIF 도 마찬가지로 포스트 디렉터리에 둡니다. 
- URL 슬러그는 디렉터리명의 공백을 `-` 로 바꾼 값입니다. `src/content.config.ts` 의 `generateId` 가 결정합니다.

### 세로로 긴 이미지

화면에서 세로 900px 을 넘기면 `div` 로 감싸 표시 크기를 줄입니다. 앞뒤에 빈 줄을 둬야 안쪽이 마크다운으로 처리됩니다.

```mdx
<div class="img-sm">

![alt](<./파일명.png>)

</div>
```


| 클래스      | 표시 폭  | 쓰는 곳          |
| -------- | ----- | ------------- |
| `img-sm` | 360px | 모바일과 태블릿 스크린샷 |
| `img-md` | 560px | 세로로 긴 데스크톱 화면 |
| 없음       | 본문 폭  | 나머지 전부        |


## 문장 규칙

본문과 썸네일 문구에 똑같이 적용합니다.

### 1. 불필요한 쉼표를 쓰지 않는다

연결 어미 뒤에는 쉼표를 찍지 않습니다. 읽는 흐름이 끊기는 자리가 아니면 그냥 붙여 씁니다.

쉼표를 찍고 싶어지면 대개 문장이 길다는 신호입니다. 쉼표를 넣는 대신 문장을 둘로 나눕니다.

### 2. 가운뎃점(·)을 쓰지 않는다

두 낱말을 붙이고 싶으면 조사로 풀어 씁니다.

### 3. 한 문장에 하나만 담는다

주어와 서술어가 호응하는지 확인합니다. 세 줄을 넘어가는 문장은 나눕니다.

문단도 앞뒤가 이어지게 씁니다. 앞 문단이 남긴 질문에 다음 문단이 답하는 순서로 놓습니다.

### 4. 비유하지 않고 직접 설명한다

대상을 실제 이름으로 부릅니다. 무엇을 가리키는지 한 번 더 생각해야 하는 표현은 쓰지 않습니다. 헤딩에도 똑같이 적용합니다.

### 5. 제목은 내용을 그대로 가리키는 말로 짓는다

대구를 맞추거나 카피라이팅 투로 쓰지 않습니다.

- "네 가지 부품과 세 개의 버스" 대신 "컴퓨터의 네 가지 핵심 부품"
- "용어 여섯 개와 비유 하나" 대신 "이 글에 나오는 용어"

### 6. 정보가 없는 문장은 쓰지 않는다

독자를 다독이거나 분량을 채우는 문장은 지웁니다. "여기만 읽고 내려가면 나머지는 다 따라옵니다" 같은 문장이 여기 해당합니다.

### 7. 문장은 동사로 끝낸다

명사에 서술격 조사를 붙여 끝내지 않습니다. "해결 수단이 308입니다" 대신 "308 리다이렉트로 대응합니다" 로 씁니다.

정도를 나눌 수 없는 서술에는 강조어를 붙이지 않습니다. "완전히 비활성이다" 대신 "활성화되지 않는다" 로 씁니다.

### 8. 뜻이 바로 잡히지 않는 축약 표현을 쓰지 않는다

압축된 한자어 명사 대신 정확한 용어를 쓰거나 동작이 드러나게 풀어 씁니다.

### 9. 항목 번호는 일반 숫자로 쓴다

①②③ 같은 원문자와 ⓐ ㉠ 같은 기호 마커를 쓰지 않고 `1.` `2.` `3.` 으로 씁니다.

### 10. 화면 동작은 조작과 응답을 기준으로 쓴다

- **UI 요소를 구체적으로 지칭합니다.** "메뉴로 들어가면" 대신 "사이드바를 통해 퀵링크 탭 클릭 시" 로 씁니다.
- **조작이나 응답을 기준으로 씁니다.** "저장이 실패하면" 대신 "저장 500 응답 시" 로 씁니다.
- **기술 용어는 번역하지 않고 원어로 씁니다.** url 과 쿼리스트링과 input 과 redirect 와 localStorage 가 그렇습니다.
- **결과는 일어난 순서대로 나열합니다.** "1페이지부터 새 개수로 다시 조회한다" 대신 "노출개수가 적용되고 1페이지로 조회한다" 로 씁니다.
- **무엇이 적용되고 무엇이 적용되지 않는지 명시합니다.** "노출개수를 뺀 검색조건으로 조회하고" 대신 "노출개수가 적용되지 않고 검색조건이 반영된 데이터만 조회된다" 로 씁니다.
- **화면에서 쓰는 도메인 용어를 그대로 씁니다.** "검증 안내" 대신 "툴팁" 으로 "페이지당 개수" 대신 "페이지당 노출개수" 로 씁니다.

### 바꿔 쓰는 표현

쓰지 말라고 지적받은 표현은 이 표에 추가합니다.

| 쓰지 않는 표현 | 대신 쓰는 표현 |
| --- | --- |
| 스텁(stub) | 빈 껍데기 화면, 미구현 자리 화면 |
| 사슬, 방아쇠, 폭발 반경 | 과정, 단계, 원인, 영향 범위 |
| 코드 밖 | 인프라 설정 |
| 이름 조회 설정 | DNS 조회 |
| 폐기 | 서버 HTML 을 버리고 다시 그림 |
| 무경계 | Suspense 없음 |
| 무해 | 문제를 일으키지 않음 |
| 증적 | 테스트 기록, 실행 화면 기록 |
| 못박다 | 고정하다, 테스트로 고정하다 |
| 백지가 된다 | 아무것도 그려지지 않는다 |
| 구멍 | 처리되지 않는 경우, 문제 |
| 살아 있다 | 누를 수 있다, 동작한다 |
| 낡은 | 이전 |
| 정반대다 | 서로 반대로 동작한다 |
| 실서버 | 실제 서버 |
| 히스토리 | 뒤로가기, 방문 기록 |
| 왕복 | 거치는 지점을 화살표로 나열 (목록 → 수정 → 목록) |
| 감췄다, 숨겼다 | 처리한 방식을 그대로 씁니다. `v-if` 로 DOM 에 만들지 않았으면 "그리지 않는다" 로 CSS 로 처리했으면 "`display: none` 을 줬다" 로 씁니다 |
| 따라온다 | 무엇이 어떻게 바뀌는지 동작으로 씁니다. "색인은 따라옵니다" 대신 "구글이 재크롤 시 색인을 갱신합니다" |
| 층, 층위 | 나눈 대상을 그대로 부릅니다. "문제를 두 층으로 나누면" 대신 "이 문제는 우회 가입 위험과 검색 노출 두 가지로 나뉩니다" |
| 조건이 참이다, 거짓이다 | 조건이 성립한다, 조건에 부합한다, 조건이 성립하지 않는다 |
| 되찾았다, 돌아왔다 | 무엇이 어떤 상태가 됐는지 그대로 씁니다. "여백을 되찾았고" 대신 "위아래 여백이 생겼고" |

## 말투

- 존댓말 `~습니다` 를 기본으로 하고 `~어요` 를 섞어 톤을 낮춥니다.
- `~인데요` `~이죠` `~군요` `~지요` 같은 어미를 자연스럽게 씁니다.
- 독자에게 질문을 던지며 전개합니다. "그렇다면 왜 이렇게 동작할까요?" "확인해볼까요?"
- 1인칭 실무 시점으로 씁니다. "저희 서비스는" "제가 확인해보니"

## 사내 정보 노출

사내 사례를 쓸 때는 인프라 구성이 드러나는 사실을 그대로 적지 않습니다. 원인을 설명하는 데 필요한 만큼만 일반화해서 씁니다.

- **서버 대수와 이름 규칙을 적지 않습니다.** `m01~m04 네 대` 처럼 쓰지 않고 `여러 대` `다른 서버` 로 씁니다. 특정 서버를 가리켜야 하면 `서버 A` `서버 B` 로 부릅니다. 대수에서 나온 수치도 마찬가지입니다. `4분의 3 확률` 대신 `요청이 어느 서버로 갔느냐에 따라` 로 씁니다.
- **실제 도메인을 적지 않습니다.** `example.com` 을 씁니다. 개별 서버 주소도 `web-a.example.com` 처럼 일반적인 이름으로 바꿉니다.
- **인프라 제품과 방식을 특정하지 않습니다.** `GSLB` 처럼 구성을 짐작할 수 있는 이름 대신 `로드밸런서` 라고만 씁니다.
- **사내 식별자를 적지 않습니다.** 사내 헤더 이름과 그 값, 티켓 번호, 사내 URL 이 여기 해당합니다. 필요하면 `Service-Channel` 처럼 일반적인 이름으로 바꿉니다.
- **일반화해도 글이 성립하는지 확인합니다.** 대수나 제품명이 있어야만 설명이 되는 문장이면 그 문장을 다시 씁니다. 대개는 없어도 됩니다.

파일명과 코드는 저장소 구조가 드러나지 않는 선에서 그대로 써도 됩니다. `axiosServer.ts` 정도는 괜찮고 사내 도메인이 박힌 설정값은 바꿉니다.

## 글 구성

- `#` 은 큰 챕터에만 씁니다. 
- `##` 과 `###` 로 잘게 나눕니다. 문제점을 열거할 때는 `### 1.` 처럼 번호를 붙입니다.
- 코드 블록은 짧게 여러 개 넣습니다. 긴 코드 하나보다 낫습니다.
- 비교나 장점 정리는 표로 만듭니다.
- 용어 보충은 인용문 혹은 토글로 답니다. `> 스코프란? <br /> 변수에 접근할 수 있는 범위입니다.`
- 글 끝에는 참고문헌을 접어 둡니다.

```mdx
<details>

<summary>참고문헌</summary>

<div markdown="1">

https://example.com

</div>

</details>
```

## frontmatter

```yaml
---
date: "2026-09-01"
title: "제목"
categories: ["React", "Next.js"]
summary: "한 줄 요약입니다. ~를 공유합니다 또는 ~를 분석했습니다 로 끝냅니다."
thumbnail: "./thumbnail.png"
---
```

`src/content.config.ts` 스키마에 다섯 필드가 모두 필수로 잡혀 있습니다.

## categories 규칙

`categories` 는 태그 페이지의 분류 기준입니다. 문자열이 한 글자라도 다르면 다른 태그로 갈라지니 기존 표기를 그대로 따릅니다.

- **여러 낱말은 하이픈으로 잇습니다.** `Factory-Pattern` `React-Key` `Query-Key` `Escape-Sequence` 처럼 씁니다. 띄어쓰기는 쓰지 않습니다. 다만 공식 표기 자체에 슬래시가 들어가면 그대로 둡니다. `shadcn/ui` 가 그렇습니다.
- **공식 표기를 그대로 씁니다.** 대소문자는 그 기술이 스스로를 부르는 방식을 따릅니다. 임의로 줄이거나 대문자를 바꾸지 않습니다.
- **소문자로 시작하는 이름은 그대로 둡니다.** `nginx` `immer` `axios` `console` `cloneElement` `useOverlay` 는 공식 표기가 소문자입니다. 억지로 첫 글자를 올리지 않습니다.
- **기존 표기를 먼저 확인합니다.** 새 태그를 만들기 전에 같은 뜻의 태그가 이미 있는지 봅니다.

기존 태그 목록은 아래 명령으로 확인합니다.

```bash
grep -h '^categories:' src/content/*/index.mdx | grep -o '"[^"]*"' | sort | uniq -c | sort -rn
```

## MDX 주의사항

빌드가 통과해도 화면에서 깨지는 경우가 있으니 아래를 확인합니다.

- **중괄호**는 본문에서 JSX 표현식으로 해석됩니다. 코드 블록 밖에서는 백틱으로 감싸거나 `\{` 로 이스케이프합니다.
- **볼드 별표**는 낱말 경계에 붙여야 합니다. `**이모지(**🎉**)**` 는 별표가 그대로 노출됩니다. `**이모지**(🎉)` 로 씁니다.
- **URL 형태의 문자열**은 자동으로 링크가 됩니다. `res.data` 같은 값은 반드시 백틱으로 감쌉니다.
- **백슬래시 이스케이프 시퀀스**는 백틱 안에 넣습니다. `\ud83c\udf89` 처럼 코드 서식을 유지해야 의도가 전달됩니다.

## 썸네일

규격만 맞추면 됩니다. 배경색과 구도와 폰트는 글마다 자유롭게 정합니다.

- 1200x630 으로 만듭니다. `src/pages/[slug].astro` 가 이 값을 OG 이미지로도 넘깁니다.
- 목록 카드는 564x296 으로 폭에 맞춰 줄어들기만 하고 잘리지 않습니다. 다만 작게 보이니 글자를 너무 작게 넣지 않습니다.
- 포스트 헤더에서는 `brightness(0.25)` 가 걸린 배경으로 깔립니다. 때문에 썸네일 이미지 배경을 너무 어두운 색으로 사용하지 않습니다.

